import streamlit as st

with st.container(border = True):
                c1, c2 = st.columns(2)
                with c1:
                        st.write("")
                        st.write("")
                        st.write("")
                        st.write("")
                        st.title("Fluid")
                        st.title("Simulation")
                with c2:
                        st.image("Logo1.png")
